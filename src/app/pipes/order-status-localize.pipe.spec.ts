import { OrderStatusLocalizePipe } from './order-status-localize.pipe';

describe('OrderStatusLocalizePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderStatusLocalizePipe();
    expect(pipe).toBeTruthy();
  });
});
