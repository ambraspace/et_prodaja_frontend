import { OfferStatusLocalizePipe } from './offer-status-localize.pipe';

describe('OfferStatusLocalizePipe', () => {
  it('create an instance', () => {
    const pipe = new OfferStatusLocalizePipe();
    expect(pipe).toBeTruthy();
  });
});
