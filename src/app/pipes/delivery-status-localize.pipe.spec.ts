import { DeliveryStatusLocalizePipe } from './delivery-status-localize.pipe';

describe('DeliveryStatusLocalizePipe', () => {
  it('create an instance', () => {
    const pipe = new DeliveryStatusLocalizePipe();
    expect(pipe).toBeTruthy();
  });
});
